services:
  - type: web
    name: claim-helper
    runtime: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    autoDeploy: false
    envVars:
      - key: APP_BUILD
        value: render-prod
